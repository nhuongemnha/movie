import React from 'react';
import Layout from '../../templates/HomeTemplate';

const Profile = () => {
    return (
          <Layout>
            Profile
          </Layout>
    );
};

export default Profile;